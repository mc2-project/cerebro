Params.set_params(int_precision=3, f=3,k=6)

# Piecewise functionality
# The function is:
# [-inf, -1): f(x) = 0
# [-1, 1): f(x) = x + 1
# [1, -inf): f(x) = 1

# fn = Piecewise(3)
# fn.add_boundary(None, sfix(-1), sfix(0), sfix(0))
# fn.add_boundary(sfix(-1), sfix(1), sfix(1), sfix(1))
# fn.add_boundary(sfix(1), None, sfix(0), sfix(1))

# v = fn.evaluate(sfix(0.5))
# reveal_all(v)

