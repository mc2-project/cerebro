Params.set_params(int_precision=32, f=32, k=64)
NUM_PARTIES = 3

SGD_ITERS = 10
data_sizes = [10, 10, 10]
total_samples = sum(data_sizes)
dim = 10
BATCH_SIZE = 5

LEVELS = 5
TOTAL_NODES = 2 ** 5 - 1

# The columns are:
# feature index
# split value
# index of left child (or prediction value, if the it's a leaf node)
# index of right child
tree = s_fix_mat(TOTAL_NODES, 4)

w = tree[0]
x = s_fix_array.read_input(dim, 0)

for i in range(LEVELS):
    index = w[0]
    split = w[1]
    left_child = w[2]
    right_child = w[3]

    f = array_index_secret(x, index)
    cond = (f < split)
    print(type(cond))
    w = array_index_secret_if(f, l, left_child, right_child)

w[0].reveal()
